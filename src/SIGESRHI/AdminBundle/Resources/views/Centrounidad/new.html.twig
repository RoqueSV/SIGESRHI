{% extends '::PrincipalBase.html.twig' %}

{% block principal%}
    <h1>Centrounidad creation</h1>

    <form action="{{ path('centrounidad_create') }}" method="post" class="contact_form" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('centrounidad') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
