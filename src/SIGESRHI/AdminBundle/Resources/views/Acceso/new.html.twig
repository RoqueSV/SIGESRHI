{% extends '::PrincipalBase.html.twig' %}

{% block principal -%}
    <h1>Acceso creation</h1>

    <form action="{{ path('acceso_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('acceso') }}">
            Regresar
        </a>
    </li>
</ul>
{% endblock %}
