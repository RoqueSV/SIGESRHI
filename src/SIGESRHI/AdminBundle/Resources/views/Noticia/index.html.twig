{% extends '::PrincipalBase.html.twig' %}

{% block principal -%}
{% if app.session.started %}
    {% for flashMessage in app.session.flashbag.get('new') %}
        <div class="alert alert-success">
            <p> {{ flashMessage }} </p>
        </div>
    {% endfor %}
{% endif %}
<h4 class='text-center'>NOTICIAS ISRI</h4>
<p class="text-center">Seleccione la opción para la noticia deseada</p>
{{ grid_search(grid) }}

    {{ grid(grid,_self) }}

    {% block grid_filters %}{% endblock %}
    <br>
<div align='center'>
    <a  href="{{ path('hello_page') }}" class="btn">Cancelar</a>
    <a href="{{ path('noticia_new') }}" class="btn">
        <div class="icon-plus-sign"></div>            
    </a>
</div>
<style type="text/css">
#grid_grid_noticias__fechanoticia__query__from{
    width: 100px; 
}

.grid-filter-input .grid-filter-operator select{
    width: 185px;
    margin-left: 10px;

}
</style>
<script>
$(document).ready(function(){
         $("#grid_grid_noticias__fechanoticia__query__from").datepicker({ 
              dateFormat: 'yy-mm-dd',  
              changeMonth: true,
              changeYear: true,
              yearRange: "-100:+0",});

         $('#grid_grid_noticias__fechanoticia__query__from').attr('readonly',true);

         $('#fecha_requerida').hide();
         $('#fecha_invalida').hide();
        
    });
</script>
    {% endblock %}
