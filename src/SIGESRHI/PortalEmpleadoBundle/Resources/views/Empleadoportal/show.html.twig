{% extends '::PrincipalBaseEmpleado.html.twig' %}

{% block principal -%}
    <h1>Empleado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codigoempleado</th>
                <td>{{ entity.codigoempleado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('empleadoportal') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
