{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Solicitudcapacitacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechasolicitud</th>
                <th>Aprobacionsolicitud</th>
                <th>Motivosolicitud</th>
                <th>Comentariosolicitud</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('solicitudcapacitacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechasolicitud %}{{ entity.fechasolicitud|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.aprobacionsolicitud }}</td>
                <td>{{ entity.motivosolicitud }}</td>
                <td>{{ entity.comentariosolicitud }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('solicitudcapacitacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('solicitudcapacitacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('solicitudcapacitacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
