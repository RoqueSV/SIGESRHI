{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Factorevaluacion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrefactor</th>
                <td>{{ entity.nombrefactor }}</td>
            </tr>
            <tr>
                <th>Descripcionfactor</th>
                <td>{{ entity.descripcionfactor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('factorevaluacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('factorevaluacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('factorevaluacion_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
