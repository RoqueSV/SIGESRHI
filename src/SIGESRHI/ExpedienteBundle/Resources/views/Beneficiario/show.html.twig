{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Beneficiario</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrebeneficiario</th>
                <td>{{ entity.nombrebeneficiario }}</td>
            </tr>
            <tr>
                <th>Parentescobeneficiario</th>
                <td>{{ entity.parentescobeneficiario }}</td>
            </tr>
            <tr>
                <th>Porcentaje</th>
                <td>{{ entity.porcentaje }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('beneficiario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('beneficiario_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('beneficiario_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
