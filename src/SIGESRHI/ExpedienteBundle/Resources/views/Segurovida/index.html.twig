{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Segurovida list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechaseguro</th>
                <th>Estadoseguro</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('segurovida_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaseguro %}{{ entity.fechaseguro|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.estadoseguro }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('segurovida_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('segurovida_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('segurovida_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
