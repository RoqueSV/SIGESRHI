{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Docexpediente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredocexp</th>
                <th>Rutadocexp</th>
                <th>Fechadocexp</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('docdigital_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombredocexp }}</td>
                <td>{{ entity.rutadocexp }}</td>
                <td>{% if entity.fechadocexp %}{{ entity.fechadocexp|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('docdigital_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('docdigital_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('docdigital_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
