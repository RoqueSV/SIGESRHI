{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Expediente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechaexpediente</th>
                <td>{{ entity.fechaexpediente|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Tipoexpediente</th>
                <td>{{ entity.tipoexpediente }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('expediente') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('expediente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('expediente_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
