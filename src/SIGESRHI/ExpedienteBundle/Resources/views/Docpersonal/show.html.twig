{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Docpersonal</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombredocpersonal</th>
                <td>{{ entity.nombredocpersonal }}</td>
            </tr>
            <tr>
                <th>Entregado</th>
                <td>{{ entity.entregado }}</td>
            </tr>
            <tr>
                <th>Indice</th>
                <td>{{ entity.indice }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('docpersonal') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('docpersonal_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('docpersonal_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
