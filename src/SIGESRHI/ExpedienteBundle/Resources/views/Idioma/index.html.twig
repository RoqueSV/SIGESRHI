{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Idioma list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombreidioma</th>
                <th>Nivelescribe</th>
                <th>Nivelhabla</th>
                <th>Nivellee</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('idioma_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreidioma }}</td>
                <td>{{ entity.nivelescribe }}</td>
                <td>{{ entity.nivelhabla }}</td>
                <td>{{ entity.nivellee }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('idioma_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('idioma_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('idioma_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
