{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Empleadoconcurso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecharegistro</th>
                <th>Docconcurso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('empleadoconcurso_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fecharegistro %}{{ entity.fecharegistro|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.docconcurso }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('empleadoconcurso_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('empleadoconcurso_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('empleadoconcurso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
