{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Datosfamiliares list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombrefamiliar</th>
                <th>Direccionfamiliar</th>
                <th>Telefonofamiliar</th>
                <th>Parentesco</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('datosfamiliares_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombrefamiliar }}</td>
                <td>{{ entity.direccionfamiliar }}</td>
                <td>{{ entity.telefonofamiliar }}</td>
                <td>{{ entity.parentesco }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('datosfamiliares_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('datosfamiliares_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('datosfamiliares_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
