{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pruebapsicologica</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Resultadocoeficiente</th>
                <td>{{ entity.resultadocoeficiente }}</td>
            </tr>
            <tr>
                <th>Calificacioncoeficiente</th>
                <td>{{ entity.calificacioncoeficiente }}</td>
            </tr>
            <tr>
                <th>Resultadoafectividad</th>
                <td>{{ entity.resultadoafectividad }}</td>
            </tr>
            <tr>
                <th>Resultadorelaciones</th>
                <td>{{ entity.resultadorelaciones }}</td>
            </tr>
            <tr>
                <th>Resultadoautoreconocimiento</th>
                <td>{{ entity.resultadoautoreconocimiento }}</td>
            </tr>
            <tr>
                <th>Resultadoseguridad</th>
                <td>{{ entity.resultadoseguridad }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pruebapsicologica') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pruebapsicologica_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pruebapsicologica_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
