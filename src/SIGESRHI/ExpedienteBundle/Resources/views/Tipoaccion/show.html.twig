{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tipoaccion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombretipoaccion</th>
                <td>{{ entity.nombretipoaccion }}</td>
            </tr>
            <tr>
                <th>Descripciontipoaccion</th>
                <td>{{ entity.descripciontipoaccion }}</td>
            </tr>
            <tr>
                <th>Tipoaccion</th>
                <td>{{ entity.tipoaccion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoaccion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipoaccion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('tipoaccion_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
