{% extends '::PrincipalBase.html.twig' %}

{% block principal -%}
    <h3 class="text-center">Datos de Capacitacion</h3>

    <table class="table table-bordered table-hover">
        <tbody>
            <tr>
                <th>Temática</th>
                <td>{{ entity.tematica }}</td>
            </tr>
            <tr>
                <th>Fecha capacitacion</th>
                <td>{{ entity.fechacapacitacion|date('Y-m-d')|fecha }}</td>
            </tr>
            <tr>
                <th>Hora inicio</th>
                <td>{{ entity.horainiciocapacitacion|date('h:i a')  }}</td>
            </tr>
            <tr>
                <th>Hora fin</th>
                <td>{{ entity.horafincapacitacion|date('h:i a')  }}</td>
            </tr>
            <tr>
                <th>Facilitador</th>
                <td>{{ entity.idcapacitador.capacitadorins }}</td>
            </tr>
            <tr>
                <th>Lugar de capacitación</th>
                <td>{{ entity.lugarcapacitacion }}</td>
            </tr>
            <tr>
                <th>Area de capacitacion</th>
                <td>{{ entity.areacapacitacion }}</td>
            </tr>
            <tr>
                <th>Objetivo de la capacitación</th>
                <td>{{ entity.objetivocapacitacion }}</td>
            </tr>
            <tr>
                <th>Perfil del participante</th>
                <td>{{ entity.perfilcapacitacion }}</td>
            </tr>
            <tr>
                <th>Metodologia</th>
                <td>{{ entity.metodologia }}</td>
            </tr>
            <tr>
                <th>Resultados esperados</th>
                <td>{{ entity.resultadoscapacitacion }}</td>
            </tr>
            <tr>
                <th>Cupo</th>
                <td>{{ entity.cupo }}</td>
            </tr>
            <tr>
                <th>Material de apoyo</th>
                <td>{{ entity.materialcapacitacion }}</td>
            </tr>
            <tr>
                <th>Plazo de inscripción</th>
                <td>{{ entity.plazocapacitacion|date('Y-m-d')|fecha }}</td>
            </tr>
            <tr>
                <th>Contacto</th>
                <td>{{ entity.contactocapacitacion }}</td>
            </tr>
            
        </tbody>
    </table>

    <div class="text-center">
      <a href="{{ path('capacitacion_modificar',{'id':entity.id}) }}" class="btn btn-primary"> Modificar </a>
      <a href="{{ path('plancapacitacion_show',{'id':entity.idplan.id}) }}" class="btn"> Regresar </a>
    </div>

        <form action="{{ path('capacitacion_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            <div class="invisible">{{ form_widget(delete_form) }}</div>
        </form>

{% endblock %}
