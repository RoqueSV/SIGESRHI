{% extends '::PrincipalBase.html.twig' %}

{% block principal -%}
     
     <div class="text-center"> 
     <h2 class="text-error">Acceso denegado!</h2>
     <img src="{{asset('images/denegado.png')}}" height ="250px;" width= "250px;"/>
     <h4>Usted no tiene los permisos necesarios para acceder a esta página</h4>
     <a href="{{ path('hello_page') }}" class="btn btn-primary"> Inicio </a>
    </div>
{% endblock %}